name: Android CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up JDK 11
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'adoptopenjdk'

      # Clean Gradle Cache (optional but helpful to avoid caching issues)
      - name: Clean Gradle cache
        run: rm -rf ~/.gradle/caches/

      # Ensure gradlew has executable permission
      - name: Ensure gradlew has executable permission
        run: chmod +x gradlew

      # Install Gradle if gradlew doesn't exist
      - name: Download Gradle Wrapper
        run: |
          if [ ! -f gradlew ]; then
            echo "gradlew not found, generating it..."
            curl -sSL https://services.gradle.org/distributions/gradle-7.4.2-bin.zip -o gradle.zip
            unzip gradle.zip
            mv gradle-7.4.2 gradle
            chmod +x gradle/bin/gradle
          fi

      # Build the app
      - name: Build the app
        run: ./gradlew assembleDebug --stacktrace --warning-mode all --info --debug
